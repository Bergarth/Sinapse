<UserControl
    x:Class="DesktopShell.Views.SidebarView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:DesktopShell.ViewModels"
    mc:Ignorable="d">

    <Border Padding="16" BorderBrush="Gray" BorderThickness="1" CornerRadius="8" Margin="12">
        <StackPanel Spacing="8">
            <TextBlock FontSize="20" FontWeight="SemiBold" Text="Conversations" />
            <TextBlock Text="Open an existing conversation loaded from SQLite." TextWrapping="Wrap" />

            <ListView
                x:Name="ConversationList"
                SelectionMode="Single"
                SelectionChanged="ConversationList_SelectionChanged"
                ItemsSource="{x:Bind ViewModel.Conversations, Mode=OneWay}">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="viewModels:ConversationSummaryViewModel">
                        <StackPanel Spacing="2" Padding="4">
                            <TextBlock Text="{x:Bind Title, Mode=OneWay}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                            <TextBlock Text="{x:Bind UpdatedAt, Mode=OneWay}" FontSize="12" Opacity="0.7" />
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <TextBlock Text="{x:Bind ViewModel.StatusText, Mode=OneWay}" TextWrapping="Wrap" />
            <Button Content="Refresh" Click="Refresh_Click" HorizontalAlignment="Left" />
        </StackPanel>
    </Border>
</UserControl>
