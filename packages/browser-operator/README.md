# browser-operator

Contract-first placeholder package for browser action capabilities.

This package intentionally contains no real browser automation yet. It defines
stable method contracts the daemon can call now, so a later Playwright-backed
implementation can be added without changing daemon/shell integration points.
