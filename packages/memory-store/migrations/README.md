# migrations

SQLite migration scaffold.

## Conventions (planned)

- Numbered SQL files, e.g. `0001_init.sql`, `0002_add_tasks_index.sql`
- Up-only migration files for local-first development
- Idempotent statements where possible

No migration SQL is implemented yet.
